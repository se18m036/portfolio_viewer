name: portfolio_viewer
description: A new Flutter project.
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=2.19.0 <3.0.0"

dependencies:
  authentication_repository:
    path: packages/authentication_repository
  user_repository:
    path: packages/user_repository
  depot_repository:
    path: packages/depot_repository
  bloc: ^8.1.0
  equatable: ^2.0.3
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.1
  formz: ^0.4.0
  collection: ^1.17.0
  privacy_screen: ^0.0.6
  logger: ^1.3.0
  freerasp: ^4.0.0
  sentry_flutter: ^7.5.1
  sentry_logging: ^7.5.1

dev_dependencies:
  bloc_test: ^9.1.1
  flutter_test:
    sdk: flutter
  mocktail: ^0.3.0
  patrol: ^1.1.0
  very_good_analysis: ^3.1.0

flutter:
  uses-material-design: true

patrol:
  app_name: Portfolio Viewer
  android:
    package_name: dev.koller.portfolio_viewer
  ios:
    bundle_id: dev.koller.portfolio_viewer
